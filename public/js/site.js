document.addEventListener('DOMContentLoaded',()=>{const glow=document.getElementById('cursorGlow');window.addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));initHero();document.querySelectorAll('.pano-shell[data-panorama]').forEach(initPanorama);});
function initHero(){const canvas=document.getElementById('heroCanvas');if(!canvas||!window.THREE)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(50,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));renderer.setSize(innerWidth,innerHeight);camera.position.z=7;const group=new THREE.Group();scene.add(group);const material=new THREE.MeshBasicMaterial({color:0x8a5cff,wireframe:true,transparent:true,opacity:.36});const geo=new THREE.IcosahedronGeometry(2.1,2);const mesh=new THREE.Mesh(geo,material);group.add(mesh);const ringMat=new THREE.MeshBasicMaterial({color:0x00e5ff,wireframe:true,transparent:true,opacity:.22});for(let i=0;i<3;i++){const ring=new THREE.Mesh(new THREE.TorusGeometry(2.7+i*.45,.012,8,120),ringMat);ring.rotation.x=Math.PI/2.5+i*.35;ring.rotation.y=i*.55;group.add(ring)}const pts=[];for(let i=0;i<180;i++)pts.push((Math.random()-.5)*14,(Math.random()-.5)*10,(Math.random()-.5)*7);const pGeo=new THREE.BufferGeometry();pGeo.setAttribute('position',new THREE.Float32BufferAttribute(pts,3));scene.add(new THREE.Points(pGeo,new THREE.PointsMaterial({color:0xffffff,size:.025,transparent:true,opacity:.45})));let px=0,py=0;window.addEventListener('pointermove',e=>{px=(e.clientX/innerWidth-.5)*.5;py=(e.clientY/innerHeight-.5)*.35});function draw(){requestAnimationFrame(draw);group.rotation.y+=.0025;group.rotation.x+=(py-group.rotation.x)*.018;group.rotation.y+=(px-group.rotation.y)*.012;group.position.x=innerWidth>900?2.1:0;renderer.render(scene,camera)}draw();window.addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})}
function initPanorama(el){const url=el.dataset.panorama;if(!url||!window.THREE)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(70,el.clientWidth/el.clientHeight,.1,1000),renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(el.clientWidth,el.clientHeight);el.appendChild(renderer.domElement);const geo=new THREE.SphereGeometry(500,60,40);geo.scale(-1,1,1);new THREE.TextureLoader().load(url,tex=>{scene.add(new THREE.Mesh(geo,new THREE.MeshBasicMaterial({map:tex})));const ph=el.querySelector('.pano-placeholder');if(ph)ph.style.display='none'});let lon=0,lat=0,down=false,sx=0,sy=0,sl=0,st=0;el.addEventListener('pointerdown',e=>{down=true;sx=e.clientX;sy=e.clientY;sl=lon;st=lat;el.setPointerCapture?.(e.pointerId)});el.addEventListener('pointerup',()=>down=false);el.addEventListener('pointermove',e=>{if(down){lon=sl+(sx-e.clientX)*.12;lat=st+(e.clientY-sy)*.12}});el.addEventListener('wheel',e=>{camera.fov=Math.max(35,Math.min(90,camera.fov+e.deltaY*.03));camera.updateProjectionMatrix();e.preventDefault()},{passive:false});function draw(){requestAnimationFrame(draw);lat=Math.max(-85,Math.min(85,lat));const phi=THREE.MathUtils.degToRad(90-lat),theta=THREE.MathUtils.degToRad(lon);camera.lookAt(500*Math.sin(phi)*Math.cos(theta),500*Math.cos(phi),500*Math.sin(phi)*Math.sin(theta));renderer.render(scene,camera)}draw();new ResizeObserver(()=>{camera.aspect=el.clientWidth/el.clientHeight;camera.updateProjectionMatrix();renderer.setSize(el.clientWidth,el.clientHeight)}).observe(el)}